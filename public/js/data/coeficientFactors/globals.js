
let cfg = {
    formatter:new Intl.NumberFormat('es-ES', {
        minimumFractionDigits: 0,
        useGrouping: true
    }),
    idBrunch:0,
    coeficientFactors:[],
    idElementToEdit:0,
}

export default cfg